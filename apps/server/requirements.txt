fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.8
pydantic-settings>=2.4
sqlmodel>=0.0.21
httpx>=0.27
openai>=1.40
jsonschema>=4.23

# RAG
lancedb>=0.15
fastembed>=0.4
rank-bm25>=0.2.2
pypdf>=5.0
python-docx>=1.1

# rich editor <-> markdown <-> RAG, and website scraping
markdownify>=0.13
markdown>=3.7
beautifulsoup4>=4.12

# LiveKit server SDK (token minting, SIP/room admin).
# Also provides PyJWT (used for auth session tokens) + pbkdf2 hashing is stdlib,
# so auth needs no extra wheels — keeps the pip layer cache-stable.
livekit-api>=0.8

# Telephony: real numbers, calls, and SMS. The provider adapter (app/telephony)
# imports this lazily, so the server still boots if it is ever absent.
twilio>=9.0

# integrations: Composio (managed OAuth + tools) + MCP client (test/exec)
composio>=0.17
mcp>=1.2
# Deep agent (LangChain deepagents harness)
deepagents>=0.6.12
langchain-openai>=0.3
